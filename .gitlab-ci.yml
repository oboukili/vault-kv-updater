---
stages:
  - build
  - release

build:
  stage: build
  image:
    name: gcr.io/kaniko-project/executor:debug-v0.10.0
    entrypoint: [""]
  script:
    - >
      /kaniko/executor \
        --single-snapshot \
        --cache true \
        --cache-ttl 72h \
        --context ${CI_PROJECT_DIR} \
        --destination eu.gcr.io/malt-build/${CI_PROJECT_NAME}:${CI_COMMIT_REF_SLUG} \
        --destination eu.gcr.io/malt-build/${CI_PROJECT_NAME}:${CI_COMMIT_SHORT_SHA}
  tags:
    - kubernetes

# release:
#  script:
#    - docker create ${CI_REGISTRY_URL}:${CI_COMMIT_SHORT_SHA}
#    -
#  artifacts:
#    paths:
#      - vault-kv-updater
#    expire_in: 1 day
#  tags:
#    - kubernetes
